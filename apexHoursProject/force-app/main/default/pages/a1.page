<apex:page >
    <!--
    <aura:component controller="CredentialingRequestController" implements="flexipage:availableForRecordHome,force:hasRecordId" access="global">
    
    <aura:attribute name="credentialingRequest" type="Credentialing_Request__c" default="{ 'sobjectType': 'Credentialing_Request__c' }" />
    
    
    <aura:attribute name="isARequired" type="Boolean" default="false" />
    <aura:attribute name="isBRequired" type="Boolean" default="false" />
    <aura:attribute name="isCRequired" type="Boolean" default="false" />
    <aura:attribute name="isDRequired" type="Boolean" default="false" />

    <lightning:card title="State Regulatory Notifications" class="slds-m-around_medium">

      
        <div class="slds-grid slds-gutters slds-m-bottom_small">
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="checkbox" label="A"
                                 value="{!v.credentialingRequest.A__c}"
                                 onchange="{!c.handleCheckboxChange}" />
            </div>
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="date"
                                 aura:id="aDate"
                                 value="{!v.credentialingRequest.a__c}"
                                 required="{!v.isARequired}"
                                 class="hidden-label" />
            </div>
        </div>

        
        <div class="slds-grid slds-gutters slds-m-bottom_small">
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="checkbox" label="B"
                                 value="{!v.credentialingRequest.B__c}"
                                 onchange="{!c.handleCheckboxChange}" />
            </div>
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="date"
                                 aura:id="bDate"
                                 value="{!v.credentialingRequest.b__c}"
                                 required="{!v.isBRequired}"
                                 class="hidden-label" />
            </div>
        </div>

       
        <div class="slds-grid slds-gutters slds-m-bottom_small">
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="checkbox" label="C"
                                 value="{!v.credentialingRequest.C__c}"
                                 onchange="{!c.handleCheckboxChange}" />
            </div>
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="date"
                                 aura:id="cDate"
                                 value="{!v.credentialingRequest.c__c}"
                                 required="{!v.isCRequired}"
                                 class="hidden-label" />
            </div>
        </div>

       
        <div class="slds-grid slds-gutters">
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="checkbox" label="D"
                                 value="{!v.credentialingRequest.D__c}"
                                 onchange="{!c.handleCheckboxChange}" />
            </div>
            <div class="slds-col slds-size_1-of-2">
                <lightning:input type="date"
                                 aura:id="dDate"
                                 value="{!v.credentialingRequest.d__c}"
                                 required="{!v.isDRequired}"
                                 class="hidden-label" />
            </div>
        </div>

       
        <div class="slds-m-top_medium">
            <lightning:button label="Save" onclick="{!c.handleSave}" variant="brand"/>
        </div>

    </lightning:card>
</aura:component>

.THIS .hidden-label label {
    display: none;
}

-->
</apex:page>