<apex:page >
    <!--
    <template>
    <template if:true={showError}>
        <div style="color: red; border: 1px solid red; padding: 10px; border-radius: 5px; margin-top: 10px;">
            ⚠️ EPDB PIN Error detected. Please verify the data in this request.
        </div>
    </template>
</template>
    
    
    import { LightningElement, api, wire } from 'lwc';
import { getRecord } from 'lightning/uiRecordApi';


import PSV_LOAD_ERROR from '@salesforce/schema/Credential_Request__c.PSV_Load_Error__c';

export default class PsvErrorDisplay extends LightningElement {
    @api recordId;
    showError = false;

    @wire(getRecord, { recordId: '$recordId', fields: [PSV_LOAD_ERROR] })
    wiredRecord({ error, data }) {
        if (data) {
            const errorValue = data.fields.PSV_Load_Error__c.value;
            this.showError = errorValue === 'EPDB PIN Error';
        } else if (error) {
            console.error('Error loading record:', error);
        }
    }
}


<?xml version="1.0" encoding="UTF-8"?>
<LightningComponentBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>59.0</apiVersion>
    <isExposed>true</isExposed>
    <targets>
        <target>lightning__RecordPage</target>
    </targets>
</LightningComponentBundle>
-->

</apex:page>